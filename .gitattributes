# Webview bundles are generated by esbuild and should keep stable LF endings.
media/webview-*.js text eol=lf

# Shell deployment scripts must remain executable by Linux after Windows archives.
*.sh text eol=lf
